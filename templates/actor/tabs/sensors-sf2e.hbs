{{!-- Sensors tab (6-player) with subtab navigation --}}
<section class="tab {{tab.cssClass}}" data-group="primary" data-tab="{{tab.id}}">

    {{> "sensors-claim-prompt"}}
    {{> "sensors-captain-boost"}}
    {{> "sensors-status-bar"}}

    {{#if (or isGM (lookup (lookup roles "sensors") "isMyRole"))}}
    {{> "sensors-bda"}}
    <nav class="sub-nav">
        <a data-sub-tab="radar">{{localize "SHIPCOMBAT.SubTab.Radar"}}</a>
        <a data-sub-tab="abilities">{{localize "SHIPCOMBAT.SubTab.Abilities"}}</a>
    </nav>
    <div class="role-panels">
        <div class="tab" data-sub-tab="radar">
            {{> "sensors-radar"}}
        </div>
        <div class="tab" data-sub-tab="abilities">
            {{> "sensors-abilities-ref"}}
        </div>
    </div>
    {{/if}}

    {{> 'complete-turn' tabRoleId="sensors" turnDone=(lookup @root.sys.turnDone "sensors")}}

</section>
